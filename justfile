# Default target
# Compile and run
default: run

# Compile and run
run:
    zig build run

# Compile project with maven to ./target
build:
    zig build

# Run all tests in project
test:
    echo TODO
